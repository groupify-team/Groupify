import React from 'react';

const InviteSection = ({ tripId, currentMembers, onInviteSent }) => {
  return (
    <div className="invite-section">
      {/* TODO: Implement invite section */}
    </div>
  );
};

export default InviteSection;
