import React from 'react';

const TripFilters = ({ filters, onFilterChange, onClear }) => {
  return (
    <div className="trip-filters">
      {/* TODO: Implement trip filtering UI */}
    </div>
  );
};

export default TripFilters;
