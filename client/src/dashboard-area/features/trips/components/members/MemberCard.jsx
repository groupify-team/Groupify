import React from 'react';

const MemberCard = ({ member, isAdmin, currentUser, onAction }) => {
  return (
    <div className="member-card">
      {/* TODO: Implement member card */}
    </div>
  );
};

export default MemberCard;
