import React from 'react';

const MemberActions = ({ member, isAdmin, onPromote, onRemove }) => {
  return (
    <div className="member-actions">
      {/* TODO: Implement member action buttons */}
    </div>
  );
};

export default MemberActions;
