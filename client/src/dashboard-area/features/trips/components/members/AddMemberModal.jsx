import React from 'react';

const AddMemberModal = ({ isOpen, onClose, tripId, onMemberAdded }) => {
  return (
    <div className="add-member-modal">
      {/* TODO: Implement add member modal */}
    </div>
  );
};

export default AddMemberModal;
