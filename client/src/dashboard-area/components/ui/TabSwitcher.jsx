// Dashboard Tab Switcher - Using Shared Component
import React from "react";
import TabSwitcher from "@/shared/components/ui/TabSwitcher";

const DashboardTabSwitcher = (props) => {
  return <TabSwitcher {...props} />;
};

export default DashboardTabSwitcher;
